/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {
        fonts: {
          google: {
            families: ['@next/font-google'],
          },
        },
      },
}

module.exports = nextConfig
