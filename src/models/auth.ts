export interface SignUpProps {
    email: string;
    name: string;
    password: string;
    password_confirmation: string;
  }
  
export interface SignInProps {
    email: string;
    password: string;
}